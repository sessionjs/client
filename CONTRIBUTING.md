# Contributing to session.js

## Protobufs

Session runs on Signal protocol, so from time to time, they might update signalservice.proto file. In that case, simply run `bun run protobuf` to compile js