import type { RequestStoreBody } from '@/network/request'
import type { ResponseStore } from '@/network/response'

export async function storeMessage(body: RequestStoreBody): Promise<ResponseStore> {
  
}